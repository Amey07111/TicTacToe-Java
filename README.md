# Tic-Tac-Toe (Java Console Game) 🎮

A classic Tic-Tac-Toe game built using Core Java. This project runs in the terminal and features robust error handling.

## ✨ Features
* **2-Player Mode:** Play locally with a friend (X vs O).
* **Input Validation:** The game handles invalid inputs (like letters or symbols) without crashing.
* **Game Logic:** Automatically detects Wins (Rows, Columns, Diagonals) and Draws.

## 🚀 How to Run
1.  **Clone the repository:**
    ```bash
    git clone [https://github.com/Amey07111/TicTacToe-Java.git](https://github.com/Amey07111/TicTacToe-Java.git)
    ```
2.  **Navigate to the folder:**
    ```bash
    cd TicTacToe-Java
    ```
3.  **Compile the code:**
    ```bash
    javac TicTacToe.java
    ```
4.  **Run the game:**
    ```bash
    java TicTacToe
    ```

## 📸 Screenshot
(You can add a screenshot of your game here later!)

## 🛠️ Technologies Used
* Java (JDK)
* Scanner Class (Input Handling)
* 2D Arrays (Game Board)

---
*Created by [AMEY PAWAR]*